import { Keyboard as KeyboardIcon } from '@phosphor-icons/react/dist/ssr';

export default function Keyboard({ className }: { className?: string }) {
  return <KeyboardIcon weight="duotone" className={className} />;
}
