import { Laptop as LaptopIcon } from '@phosphor-icons/react/dist/ssr';

export default function Laptop({ className }: { className?: string }) {
  return <LaptopIcon weight="duotone" className={className} />;
}
