import { Monitor as MonitorIcon } from '@phosphor-icons/react/dist/ssr';

export default function Monitor({ className }: { className?: string }) {
  return <MonitorIcon weight="duotone" className={className} />;
}
