import { Mouse as MouseIcon } from '@phosphor-icons/react/dist/ssr';

export default function Mouse({ className }: { className?: string }) {
  return <MouseIcon weight="duotone" className={className} />;
}
