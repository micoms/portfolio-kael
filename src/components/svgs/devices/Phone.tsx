import { DeviceMobile } from '@phosphor-icons/react/dist/ssr';

export default function Phone({ className }: { className?: string }) {
  return <DeviceMobile weight="duotone" className={className} />;
}
