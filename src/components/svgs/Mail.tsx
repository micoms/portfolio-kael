import { EnvelopeSimple } from '@phosphor-icons/react/dist/ssr';

export default function Mail({ className }: { className?: string }) {
  return <EnvelopeSimple weight="bold" className={className} />;
}
