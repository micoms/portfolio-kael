import { LinkedinLogo } from '@phosphor-icons/react/dist/ssr';

export default function LinkedIn({ className }: { className?: string }) {
  return <LinkedinLogo weight="fill" className={className} />;
}
